apiVersion: v1
kind: Template
metadata:
  name: iam
objects:
  - apiVersion: cloud.redhat.com/v1alpha1
    kind: Frontend
    metadata:
      name: iam
    spec:
      envName: ${ENV_NAME}
      title: "iam console"
      deploymentRepo: https://gitlab.cee.redhat.com/it-user/admin-self-service
      API:
        versions:
          - v1
      frontend:
        paths:
          - /apps/iam
      image: ${IMAGE}:${IMAGE_TAG}
      navItems:
        - appId: "iam"
          title: "Authentication Factors"
          href: "/iam/authentication-policy/authentication-factors"
          product: "Identity & Access Management"
      module:
        manifestLocation: "/apps/iam/fed-mods.json"
        modules:
          - id: "authentication-policy"
            module: "./RootApp"
            routes:
              - pathname: /iam/authentication-policy
parameters:
  - name: ENV_NAME
    required: true
  - name: IMAGE_TAG
    required: true
  - name: IMAGE
    value: quay.io/cloudservices/iam-console
